<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Verbatim" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="turbo-refresh-method" content="morph">
    <meta name="turbo-refresh-scroll" content="preserve">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Crimson+Pro:ital,wght@0,400;0,500;0,600;1,400&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-stone-50 text-stone-900 antialiased">
    <% unless content_for?(:hide_header) %>
      <!-- Site Header -->
      <header class="bg-stone-900 px-6 py-6">
        <div class="flex items-center justify-between max-w-6xl mx-auto">
          <%= link_to root_path, class: "inline-block" do %>
            <h1 class="font-serif text-5xl font-medium tracking-tight text-white">Verbatim</h1>
            <p class="text-sm text-stone-400">A typing practice app for reading books</p>
          <% end %>
          <% if current_user %>
            <%= button_to "Logout", logout_path, method: :delete, class: "px-4 py-1.5 text-sm text-stone-300 border border-stone-600 hover:border-stone-400 hover:text-white rounded transition-colors cursor-pointer" %>
          <% end %>
        </div>
      </header>
    <% end %>

    <% if notice.present? %>
      <div class="fixed top-4 right-4 z-50 px-4 py-3 bg-white border border-green-200 rounded-lg shadow-lg flex items-center gap-3">
        <svg class="w-5 h-5 text-green-500 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
        </svg>
        <p class="text-sm text-stone-700"><%= notice %></p>
      </div>
    <% end %>

    <% if alert.present? %>
      <div class="fixed top-4 right-4 z-50 px-4 py-3 bg-white border border-amber-200 rounded-lg shadow-lg flex items-center gap-3">
        <svg class="w-5 h-5 text-amber-500 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"/>
        </svg>
        <p class="text-sm text-stone-700"><%= alert %></p>
      </div>
    <% end %>

    <%= yield %>
  </body>
</html>
