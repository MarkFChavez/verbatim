<div class="max-w-4xl mx-auto px-6 py-12">
  <% if @book.completed? %>
    <div class="bg-emerald-50 border border-emerald-200 rounded-lg p-4 mb-8 flex items-center justify-between">
      <div class="flex items-center gap-3">
        <svg class="w-5 h-5 text-emerald-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
        </svg>
        <span class="text-emerald-800 font-medium">You've completed this book!</span>
      </div>
      <%= button_to "Restart Reading", restart_book_path(@book), method: :post, data: { turbo_confirm: "This will reset all your progress. Are you sure?" }, class: "px-4 py-2 text-sm text-emerald-700 hover:text-emerald-900 font-medium cursor-pointer" %>
    </div>
  <% end %>

  <nav class="mb-8">
    <%= link_to books_path, class: "text-sm text-stone-500 hover:text-stone-700 flex items-center gap-1" do %>
      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
      </svg>
      Back to Library
    <% end %>
  </nav>

  <header class="flex gap-8 mb-12">
    <div class="w-40 flex-shrink-0">
      <div class="aspect-[2/3] bg-stone-100 rounded-sm overflow-hidden shadow-md">
        <% if @book.cover_image.attached? %>
          <%= image_tag @book.cover_image, class: "w-full h-full object-cover" %>
        <% else %>
          <div class="w-full h-full flex items-center justify-center text-stone-400">
            <svg class="w-12 h-12" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253" />
            </svg>
          </div>
        <% end %>
      </div>
    </div>

    <div class="flex-1">
      <h1 class="text-3xl font-medium tracking-tight text-stone-900 mb-2"><%= @book.title %></h1>
      <p class="text-lg text-stone-500 mb-6"><%= @book.author %></p>

      <div class="flex items-center gap-4 mb-6">
        <div class="flex-1 max-w-xs">
          <div class="flex items-center gap-2 mb-1">
            <span class="text-sm text-stone-500">Progress</span>
            <span class="text-sm font-mono text-stone-700"><%= @book.progress_percentage %>%</span>
          </div>
          <div class="h-2 bg-stone-200 rounded-full overflow-hidden">
            <div class="h-full bg-stone-700 rounded-full transition-all" style="width: <%= @book.progress_percentage %>%"></div>
          </div>
        </div>
      </div>

      <div class="flex items-center gap-4 text-sm text-stone-500 mb-4">
        <span><%= @book.chapters.count %> chapters</span>
        <span class="text-stone-300">|</span>
        <span><%= @book.passages.count %> passages</span>
        <span class="text-stone-300">|</span>
        <span><%= number_with_delimiter(@book.total_words) %> words</span>
      </div>

      <% if @book.passages.joins(:typing_sessions).exists? %>
        <div class="flex items-center gap-6 mb-8">
          <div>
            <span class="text-2xl font-mono font-medium text-stone-900"><%= @book.average_wpm %></span>
            <span class="text-sm text-stone-500 ml-1">avg wpm</span>
          </div>
          <div>
            <span class="text-2xl font-mono font-medium text-stone-900"><%= @book.average_accuracy %>%</span>
            <span class="text-sm text-stone-500 ml-1">accuracy</span>
          </div>
        </div>
      <% else %>
        <div class="mb-8"></div>
      <% end %>

      <div class="flex items-center gap-3">
        <% unless @book.completed? %>
          <%= link_to practice_book_path(@book), class: "px-5 py-2.5 bg-stone-900 text-white font-medium rounded hover:bg-stone-800 transition-colors cursor-pointer" do %>
            <%= @book.progress_percentage > 0 ? "Continue Reading" : "Start Reading" %>
          <% end %>
        <% end %>

        <%= button_to book_path(@book), method: :delete, data: { turbo_confirm: "Delete this book?" }, class: "group px-4 py-2 border border-stone-300 text-stone-600 hover:border-red-300 hover:bg-red-50 hover:text-red-700 rounded transition-colors flex items-center gap-2 text-sm font-medium cursor-pointer" do %>
          <svg class="w-4 h-4 text-stone-400 group-hover:text-red-600 transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16" />
          </svg>
          Delete
        <% end %>
      </div>
    </div>
  </header>

  <!-- Search Section -->
  <section class="mb-8">
    <div class="flex items-center gap-2 mb-4">
      <svg class="w-4 h-4 text-stone-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
      </svg>
      <span class="text-sm font-medium text-stone-500">Find passage</span>
    </div>
    <%= form_with url: search_book_path(@book), method: :get, data: { turbo_frame: "search-results", controller: "search", action: "input->search#search" } do |f| %>
      <%= f.text_field :q,
          placeholder: "Search for text in this book...",
          class: "w-full px-4 py-2.5 border border-stone-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-stone-900 focus:border-transparent",
          data: { search_target: "input" },
          autocomplete: "off" %>
    <% end %>
    <div class="mt-3">
      <turbo-frame id="search-results"></turbo-frame>
    </div>
  </section>

  <% if @current_passage %>
    <section class="mb-8">
      <div class="flex items-center gap-2 mb-4">
        <span class="text-sm font-medium text-stone-500">Continue from here</span>
      </div>
      <h2 class="text-xl font-medium text-stone-900 mb-4">
        <%= @current_passage.chapter.title.presence || "Chapter #{@current_passage.chapter.position}" %>
      </h2>
      <div class="border border-stone-200 border-l-4 border-l-stone-700 rounded-lg p-6 bg-stone-50">
        <p class="text-sm text-stone-500 mb-3">Passage <%= @current_passage.position %></p>
        <p class="text-stone-700 line-clamp-3"><%= truncate(@current_passage.content, length: 200) %></p>
      </div>
    </section>
  <% else %>
    <section class="mb-8">
      <p class="text-stone-500">No passages in this book.</p>
    </section>
  <% end %>
</div>

